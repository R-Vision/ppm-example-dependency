// ppm_example_dependency
syntax = "proto3";

package ppm_example.dependency.v1;

// Protocol enumeration
enum Protocol {
  // Not specified
  PROTOCOL_UNSPECIFIED = 0;

  // HTTP
  PROTOCOL_HTTP = 1;

  // TCP
  PROTOCOL_TCP = 2;
}